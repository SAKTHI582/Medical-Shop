# Example inventory for an Ubuntu administration/deployment host.
# Replace the address and user before running Ansible.
[devops]
# devops-host ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/medical-shop.pem

[all:vars]
ansible_python_interpreter=/usr/bin/python3
