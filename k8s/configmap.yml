apiVersion: v1
kind: ConfigMap
metadata:
  name: medical-shop-config
  namespace: medical-shop
data:
  MYSQL_HOST: mysql
  MYSQL_PORT: "3306"
  MYSQL_USER: root
  MYSQL_DB: product_expiry_db
  MAIL_SERVER: smtp.gmail.com
  MAIL_PORT: "587"
  MAIL_USE_TLS: "true"
  GUNICORN_WORKERS: "2"
